#include <stdio.h>

float x = -3.4e-6;

int main(){
  printf("el valor de x es %f \n", x);
  printf("con dos decimales %0.2f \n", x);
  printf("con un solo decimal %0.1f \n", x);
  return 0;
}
